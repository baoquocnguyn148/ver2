version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static portfolio site - no build step required"
  artifacts:
    baseDirectory: .
    files:
      - index.html
      - docs/images/**/*
  cache:
    paths: []
